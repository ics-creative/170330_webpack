{
  "name": "_tools",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.6.2"
  },
  "scripts": {
    "format": "prettier --write '../*/src/*.{js,ts}' && prettier --write '../*/webpack.config.js'"
  }
}
